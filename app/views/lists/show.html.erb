<p id="notice"><%= notice %></p>

<%= render :partial => "prices" %>

<ul>
	<% @list.products.each do |product| %>
		<% product.offers.each do |offer| %>
			<li><%= offer.supplier.name %>, <%= offer.price %></li>
		<% end %>
	<% end %>
</ul>


<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
